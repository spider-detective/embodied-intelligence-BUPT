# 数据采集训练整体流程规范

## 1.数据采集
```
按照官方数采流程进行，之后将包上传至服务器中进行保存

第1步 上传   rsync -avP \~/bagfiles/bags a100:/ssd/bags/

第2步 登录   ssh a100

第3步 解析
cd /ssd/bags/bags/
for tar_file in case2*.tar; do
    echo "正在解析: $tar_file"
    python3 /ssd/data_parse_ros2.py "$tar_file" /path/to/output/
done

case2为网页的任务名，这个循环对所有名字包含case2的文件夹生效，改成case3则对所有名字包含case3的文件夹生效

第4步 查看   ls -lh /ssd/parsed_out/<数据包名>/
```
> \[!CAUTION]
>
> 对于第三步解析，需要在服务器中先取消conda环境后进行操作，即执行`conda deactivate`至出base环境

## 2.数据转换至lerobot格式

使用convert脚本进行转换，其中转换脚本位于`/ssd/converter/jing_convert_json_2_lerobot.py`，环境为conda（wallx）环境中

使用方法如下：

```terminal
python jing_convert_json_2_lerobot.py \
  --source-root /ssd/parsed_out_case/ \
  --dataset-root /path/to/output/ \
  --repo-id local/case \
  --task "case" \
  --append
```
```tips
这个命令是对追加数据生效的，如果需要新建一个输出文件夹需要删掉最后一行`--append`
同一个任务需要Repo-id和task都统一
```

| 参数名称         | 内容               |
| ------------ | ---------------- |
| source-root  | 上传数据集位置          |
| Dataset-root | 输出lerobot格式数据集位置 |
| Repo-id      | 自定义，/local/采集名称  |
| task         | 自定义              |

## 3.模型微调

### 基于wall-x-os-0.5模型

**（1）使用刚才已经准备好的lerobot数据集**

**（2）编写对应训练配置yml文件，放置于**`/ssd/wall-x/workspace/example**`中，命名建议与采集任务名称一致，配置文件共有两种写法，一种为发送相对量，如官方配置文件所示：\*\*

```yml
model_type: qwen2_5

task:
  # Dual-arm 6D relative action: 10 + 10 = 20, plus action_padding(6) = 26.
  dof_config:
    follow_left_ee_cartesian_pos_relative: 3
    follow_left_ee_rotation_6D_relative: 6
    follow_left_gripper: 1
    follow_right_ee_cartesian_pos_relative: 3
    follow_right_ee_rotation_6D_relative: 6
    follow_right_gripper: 1
    action_padding: 6
  ar_dof_config:
    follow_left_ee_cartesian_pos_relative: 3
    follow_left_ee_rotation_6D_relative: 6
    follow_left_gripper: 1
    follow_right_ee_cartesian_pos_relative: 3
    follow_right_ee_rotation_6D_relative: 6
    follow_right_gripper: 1
    action_padding: 6
  agent_pos_config:
    follow_left_ee_cartesian_pos: 3
    follow_left_ee_rotation_6D: 6
    follow_left_gripper: 1
    follow_right_ee_cartesian_pos: 3
    follow_right_ee_rotation_6D: 6
    follow_right_gripper: 1
    action_padding: 6
  action_horizon: 32
  action_horizon_flow: 32
  use_state_string_representation: false

model:
  backbone: qwen2_5
  config_path: /path/to/wall-oss-0.5/config.json
  processor_path: /path/to/Qwen2.5-VL-3B-Instruct
  pretrained_path: /path/to/Qwen2.5-VL-3B-Instruct
  attn_deterministic: true
  use_ema: false
  flow_loss_weight: 1.0
  ar_loss_weight: 0.01

hyperparams:
  num_epoch: 100
  batch_size_per_gpu: 4
  gradient_accumulation_steps: 4
  seed: 10222
  optimizer:
    optimizer_type: adamw
    learning_rate: 5.0e-05
    max_grad_norm: 1.0
    enable_grad_clip: true
    betas: [0.9, 0.95]
    weight_decay: 1.0e-8
    eps: 1.0e-8
  scheduler:
    scheduler_type: cosine
    num_warmup_steps: 1000
    num_training_steps: 200000
    min_lr: 1.0e-6

distributed:
  use_fsdp: true
  use_mixed_precision: true
  bf16: true

data:
  dataset_type: lerobot
  lerobot_config:
    repo_id: /path/to/cvpr_4tasks
    root: null
  key_mappings:
    camera:
      observation.images.faceImg: face_view
      observation.images.leftImg: left_wrist_view
      observation.images.rightImg: right_wrist_view
    state: observation.state
    action: action
  norm_stats_path: /path/to/cvpr_4tasks_norm_stats.json
  train_test_split: 0.95
  num_workers: 4
  max_length: 1024
  resolution:
    face_view: 448
    left_wrist_view: 448
    right_wrist_view: 448

logging:
  log_name: maniparena_ft
  log_project: lerobot_maniparena_ft
  log_entity: your_wandb_entity
  use_wandb: true
  log_interval: 10
  save_interval: 2000
  val_interval: 1000000
  epoch_save_interval: 1

checkpoint:
  save_path: /path/to/cvpr_4tasks
  # Single-file .safetensors loads as pretrain weights before FSDP wrapping.
  # Use a checkpoint directory for full resume (optimizer / scheduler / RNG).
  resume_from: /path/to/wall-oss-0.5/model.safetensors

debug:
  profile: false
  nvtx: false
```

另一种为发送绝对量：

```
```

> \[!WARNING]
>
> 注意两种配置文件会影响最后模型训练即推理方法，绝对量和相对量checkpoint和配置文件及数据不可混用！！！

**（3）进行数据归一化，在（wallx）环境中，回到文件夹/ssd/wall-x/执行**

```terminal
python scripts/compute_norm_stats.py \
  --train_config /path/to/my_robot_config.yml \
  --data_root /path/to/your_robot_dataset \
  --output_path /path/to/your_robot_norm_stats.json
```

| 参数名称          | 说明           |
| ------------- | ------------ |
| train_config | （2）中创建的配置文件  |
| data_root    | lerobot数据集位置 |
| output_path  | 输出位置         |

**（4）启动模型微调**

```terminal
CUDA_VISIBLE_DEVICES=0,1,2,3 \
  torchrun --nproc_per_node=4 \
    wall_x/trainer/fsdp_trainer/train_fsdp.py \
    --config /path/to/my_robot_config.yml
```

| 参数名称                           | 说明               |
| ------------------------------ | ---------------- |
| CUDA_VISIBLE_DEVICES         | 启用第i张显卡          |
| nproc_per_node               | 一共使用几张显卡，与上面数量对应 |
| /path/to/my_robot_config.yml | （2）中配置文件         |

**（5）开启推理**

```terminal
bash scripts/run_serving.sh \
  --checkpoint-path /path/to/checkpoint \
  --train-config-path /path/to/my_robot_config.yml \
  --port 32194 \
  --host 0.0.0.0 \
  --action-horizon 32
```

| 参数名称              | 说明           |
| ----------------- | ------------ |
| checkpoint-path   | 微调后检查点文件夹    |
| train-config-path | （2）中配置文件     |
| port              | 端口号，固定为32194 |
| action-horizon | CHUNK，一次执行的步数，16或32 |

**（6）开环图测试**

```terminal
python scripts/draw_openloop_plot.py \
  --uri ws://127.0.0.1:32194 \
  --dataset-root /path/to/your_robot_dataset \
  --train-config /path/to/my_robot_config.yml \
  --episode-indices 0,1,2 \
  --save-dir ./openloop_plots
```

| 参数名称            | 说明                       |
| --------------- | ------------------------ |
| uri             | 固定为ws\://127.0.0.1:32194 |
| dataset-root    | 转换的lerobot数据集            |
| train-config    | （2）中生成的配置文件              |
| episode-indices | 测试第几个episode             |
| save-dir        | 开环图保存文件夹                 |

**（7）上机验证**

在服务上开启推理，在数采平台按照教程开启推理即可

> \[!CAUTION]
>
> 在task instruction描述中不能出现中文
> 在机械臂主机桌面上有标准的instruction描述文档

